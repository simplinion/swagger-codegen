pipelines:
    tags:
        V.*:
            - step:
                name: Building of builder image
                trigger: automatic
                script:
                    - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"
                    - ./install-choco-scripts.sh
                    - ./build.sh --version="${BITBUCKET_TAG/V./}"
options:
    docker: true
